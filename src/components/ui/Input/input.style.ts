const commonStyles = 'block border-2 outline-none pl-1 rounded-lg';

export const styles = {
  default: `${commonStyles} focus:border-green-500 focus:bg-green-100`,
  warning: `${commonStyles} bg-red-900`,
}